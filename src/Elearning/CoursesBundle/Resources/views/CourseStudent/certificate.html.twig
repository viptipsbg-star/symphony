<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
</head>
<body>

<style type="text/css">
    html {
        width:100%;
        height:100%;
        margin:0;
        padding:0;
    }
    body {
        position:relative;
        width:595px;
        height:842px;
        margin:0 auto;
        page-break-before: avoid;
        page-break-after: avoid;
    }
    img {
        width:100%;
        height:100%;
        display:block;
    }
    .data {
        font-family:"Lucida Sans Unicode", "Lucida Grande", Verdana, Arial, Helvetica, sans-serif;
        font-size:14px;
    }
    .data .code {
        position: absolute;
        top: 378px;
        left: 268px;
    }
    .data .name {
        position: absolute;
        top: 515px;
        left: 0;
        right: 0;
        margin: auto;
        text-align: center;
        width: 330px;
    }
    .data .course {
        position: absolute;
        top: 552px;
        left: 250px;
        margin: auto;
        text-align: center;
    }
    .data .date-year {
        position: absolute;
        top: 586px;
        left: 242px;
    }
    .data .date-month {
        position: absolute;
        top: 586px;
        left: 298px;
    }
    .data .date-day {
        position: absolute;
        top: 586px;
        left: 338px;
    }
</style>
<img src="{{ asset('images/' ~ company_info.cert_image, absolute=true) }}" />
<div class="data">
    <div class="code">{{ code }}</div>
    <div class="name">{{ namesurname }}</div>
    <div class="course">{{ coursetitle }}</div>
    <div class="date-year">{{ completiondate|date("Y") }}</div>
    <div class="date-month">{{ completiondate|date("m") }}</div>
    <div class="date-day">{{ completiondate|date("d") }}</div>
</div>

<div class="data">
</div>

</body>
</html>
